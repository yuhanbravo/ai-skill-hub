interface:
  display_name: "Update Project Status"
  short_description: "Build status reports from Git and external task sources."
  default_prompt: "Generate a project status update from Git history and configured task sources, preview side effects when needed, and sync the result only through approved targets."
